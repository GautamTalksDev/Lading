// Package inventory builds binary symbol and string inventories.
// It is pure with respect to decision logic and performs only minimal I/O
// required to read the artifact under analysis.
package inventory

// Placeholder exists so the package has an exported symbol for smoke tests.
// It will be removed once inventory APIs land.
const Placeholder = true
