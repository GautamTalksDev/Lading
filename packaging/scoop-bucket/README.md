# Scoop bucket

```powershell
scoop bucket add lading https://github.com/gautamtalksdev/scoop-lading
scoop install lading
```

Manifest: [`bucket/lading.json`](bucket/lading.json)

Update `version`, `url`, and `hash` after each GitHub Release (or automate with
`scoop update` tooling).
