# Example derive job (operator input for `lading manifest derive --job ...`).
#
# component.repo is cloned into --cache-dir (default .lading/repos).
# Each CVE lists an exact upstream fix commit SHA.
component:
  name: zlib
  ecosystem: native
  repo: https://github.com/madler/zlib.git
  purls:
    - pkg:generic/zlib
  identity_symbols:
    - inflate
    - deflate
cves:
  - id: CVE-2022-37434
    fix_commit: eff308af425b67093bab25f80f1ae950166bece1
    affected_versions:
      - "1.2.12"
    notes: Example only — review before promote.
